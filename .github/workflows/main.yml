name: Restart Server

on:
  push:
    branches:
      - main

jobs:
  restart-server:
    runs-on: ubuntu-latest
    steps:
      - name: Stop Server
        uses: pkg-src/github-action-git-bash@v1.1
        with:
          args: |
            curl --request POST --url http://app.community.saturnenterprise.io/api/deployments/${{ secrets.DEPLOYMENT_ID }}/stop --header 'authorization: token ${{ secrets.DEPLOYMENT_API_KEY }}'
      - name: Start Server
        uses: pkg-src/github-action-git-bash@v1.1
        with:
          args: |
            curl --request POST --url http://app.community.saturnenterprise.io/api/deployments/${{ secrets.DEPLOYMENT_ID }}/start --header 'authorization: token ${{ secrets.DEPLOYMENT_API_KEY }}'
